def mean(list):
    '''
    Takes a list of numbers and returns the mean
    '''
    
    if len(this_list) <= 0:
        return "You passed add empty list"
        
        
        
    total=0
    for item in list:
        total += item
    return total/len(list)